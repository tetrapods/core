package io.tetrapod.core.registry;

import org.junit.*;

public class RegistryTest {

   @Test
   public void testTopics() {
      //      Topic topic = new Topic(1000, 1);
      //      topic.subscribe(100, 1, false);
      //      topic.subscribe(100, 1, false);
      //      topic.subscribe(200, 1, false);
      //      topic.subscribe(300, 1, false);
      //      topic.subscribe(100, 1, false);
      //      topic.subscribe(400, 1, false);
      //      topic.subscribe(300, 1, false);
      //      Assert.assertEquals(4, topic.getNumScubscribers());
      //      Assert.assertEquals(false, topic.unsubscribe(100, 1, false, false));
      //      Assert.assertEquals(4, topic.getNumScubscribers());
      //      Assert.assertEquals(true, topic.unsubscribe(400, 1, false, false));
      //      Assert.assertEquals(3, topic.getNumScubscribers());
      //      Assert.assertEquals(false, topic.unsubscribe(100, 1, false, false));
      //      Assert.assertEquals(3, topic.getNumScubscribers());
      //      Assert.assertEquals(true, topic.unsubscribe(100, 1, false, false));
      //      Assert.assertEquals(2, topic.getNumScubscribers());
   }

}
